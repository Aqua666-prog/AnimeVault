package com.sergey.animevault.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * AnimeVault "Nocturne / Obsidian / Lumen" palette.
 *
 * The redesign deliberately keeps the product dark-first, but drops the old
 * saturated gamer look. Most of the interface is graphite, smoke and ink;
 * colour is reserved for hierarchy and state.
 */
val VaultViolet = Color(0xFFB9A7FF)
val VaultVioletBright = Color(0xFFCCBEFF)
val VaultVioletContainer = Color(0xFF2C2750)
val VaultAqua = Color(0xFF72DCCF)
val VaultAquaContainer = Color(0xFF153D3A)
val VaultRose = Color(0xFFF19AB2)
val VaultRoseContainer = Color(0xFF4A2836)
val VaultGold = Color(0xFFE7C985)

val VaultNight = Color(0xFF080A0F)
val VaultNightDeep = Color(0xFF040509)
val VaultNightMiddle = Color(0xFF0D1017)
val VaultSurface = Color(0xFF11151D)
val VaultSurfaceLow = Color(0xFF0D1118)
val VaultSurfaceHigh = Color(0xFF191E28)
val VaultSurfaceHighest = Color(0xFF222835)
val VaultSurfaceGlass = Color(0xE60F141C)

val VaultWhite = Color(0xFFF4F3F7)
val VaultInk = Color(0xFF0B0B10)
val VaultMuted = Color(0xFFB6BAC7)
val VaultMutedDim = Color(0xFF858B9B)
val VaultOutline = Color(0xFF414858)
val VaultOutlineSoft = Color(0xFF252B37)
val VaultHairline = Color(0xFF323846)

// Compatibility aliases used by older composables.
val VaultLavender = VaultViolet
val VaultLavenderContainer = VaultVioletContainer
